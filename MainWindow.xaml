<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SPO Version Trimmer - Device Login" Height="950" Width="1000"
        WindowStartupLocation="CenterScreen" Background="#FFF5F5F5"
        MinHeight="800" MinWidth="900">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="20,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Background" Value="#FF0078D4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF005A9E"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#FFCCCCCC"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="BorderBrush" Value="#FFCCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style TargetType="GroupBox">
            <Setter Property="Margin" Value="10,8"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#FFDDDDDD"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="#FF0078D4" CornerRadius="4" Margin="0,0,0,15">
            <StackPanel Margin="20,15">
                <TextBlock Text="SharePoint Online Version Trimmer" Foreground="White" FontSize="22" FontWeight="Bold"/>
                <TextBlock Text="Device Login Edition - With Progress Tracking" Foreground="#FFE0E0E0" FontSize="12" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>
        
        <!-- Connection Panel -->
        <GroupBox Grid.Row="1" Header="Step 1: Connection Settings" Margin="0,0,0,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="130"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <Label Grid.Row="0" Grid.Column="0" Content="Site URL:" VerticalAlignment="Center"/>
                <TextBox x:Name="txtSiteUrl" Grid.Row="0" Grid.Column="1" Text="https://"/>
                
                <Label Grid.Row="1" Grid.Column="0" Content="Client ID:" VerticalAlignment="Center"/>
                <TextBox x:Name="txtClientId" Grid.Row="1" Grid.Column="1"/>
                
                <Label Grid.Row="2" Grid.Column="0" Content="Tenant ID:" VerticalAlignment="Center"/>
                <TextBox x:Name="txtTenantId" Grid.Row="2" Grid.Column="1"/>
                
                <Button x:Name="btnConnect" Grid.Row="3" Grid.Column="1" Content="Connect to SharePoint" 
                        HorizontalAlignment="Left" Width="220" Margin="5,10,5,5"/>
            </Grid>
        </GroupBox>
        
        <!-- Device Code Display Panel -->
        <Border x:Name="brdDeviceCode" Grid.Row="2" Background="#FFFFF3CD" BorderBrush="#FFE0A800" 
                BorderThickness="2" CornerRadius="4" Margin="0,0,0,10" Visibility="Collapsed" Padding="20">
            <StackPanel>
                <TextBlock Text="Authentication Required" FontSize="20" FontWeight="Bold" 
                          Foreground="#FF856404" HorizontalAlignment="Center" Margin="0,0,0,15"/>
                
                <TextBlock Text="Enter this code at microsoft.com/devicelogin:" 
                          HorizontalAlignment="Center" Margin="0,0,0,10" FontSize="14" Foreground="#FF856404"/>
                
                <Border Background="White" BorderBrush="#FFE0A800" BorderThickness="1" 
                        CornerRadius="4" Margin="50,5" Padding="15">
                    <TextBlock x:Name="txtDeviceCode" Text="XXXX-XXXX" FontSize="36" FontWeight="Bold" 
                              FontFamily="Consolas" Foreground="#FF107C10" 
                              HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,0">
                    <Button x:Name="btnOpenBrowser" Content="Open Browser" Width="150" Margin="5"/>
                    <Button x:Name="btnCopyCode" Content="Copy Code" Width="150" Margin="5" Background="#FF6C757D"/>
                </StackPanel>
                
                <TextBlock x:Name="txtAuthStatus" Text="Waiting for authentication..." 
                          HorizontalAlignment="Center" Margin="0,15,0,0" FontSize="13"
                          FontStyle="Italic" Foreground="#FF856404" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>
        
        <!-- Configuration -->
        <GroupBox Grid.Row="3" Header="Step 2: Trim Configuration" IsEnabled="False" x:Name="grpConfig" Margin="0,0,0,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,5">
                    <Label Content="Library:"/>
                    <ComboBox x:Name="cmbLibraries" Width="350" Height="32" Margin="5" VerticalContentAlignment="Center"/>
                    <Button x:Name="btnRefresh" Content="Refresh List" Width="120" Margin="5"/>
                </StackPanel>
                
                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,5">
                    <Label Content="Keep Versions:"/>
                    <TextBox x:Name="txtKeepVersions" Text="10" Width="60" TextAlignment="Center"/>
                    
                    <Label Content="Batch Size:" Margin="20,0,0,0"/>
                    <TextBox x:Name="txtBatchSize" Text="100" Width="60" TextAlignment="Center"/>
                    
                    <CheckBox x:Name="chkWhatIf" Content="SIMULATION ONLY (What-If)" IsChecked="True" 
                              VerticalAlignment="Center" Margin="30,0,0,0" Foreground="#FFCC0000" FontWeight="Bold"/>
                </StackPanel>
            </Grid>
        </GroupBox>
        
        <!-- Action Button -->
        <Button x:Name="btnTrim" Grid.Row="4" Content="START VERSION TRIM" VerticalAlignment="Top"
                Width="280" Height="50" FontSize="16" Background="#FF107C10" 
                IsEnabled="False" HorizontalAlignment="Center" Margin="0,10"/>
        
        <!-- Progress Bar -->
        <Grid Grid.Row="5" Margin="10,0,10,5" Height="30">
            <ProgressBar x:Name="prgTrimProgress" Height="25" Minimum="0" Maximum="100" Value="0" 
                        Visibility="Hidden"/>
            <TextBlock x:Name="txtProgressPercent" Text="" HorizontalAlignment="Center" 
                      VerticalAlignment="Center" FontWeight="Bold" Visibility="Hidden"/>
        </Grid>
        
        <!-- Activity Log -->
        <GroupBox Grid.Row="6" Header="Activity Log" Margin="10,5,10,10">
            <TextBox x:Name="txtLog" IsReadOnly="True" 
                    FontFamily="Consolas" FontSize="11"
                    VerticalScrollBarVisibility="Auto" 
                    HorizontalScrollBarVisibility="Auto"
                    TextWrapping="Wrap" 
                    Background="#FF1E1E1E" 
                    Foreground="#FF00FF00" 
                    Padding="10"
                    AcceptsReturn="True"/>
        </GroupBox>
    </Grid>
</Window>