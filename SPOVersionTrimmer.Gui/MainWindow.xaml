<Window x:Class="SPOVersionTrimmer.Gui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="SharePoint Online Version Trimmer" 
        Height="700" Width="900"
        WindowStartupLocation="CenterScreen">
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Connection Settings -->
        <GroupBox Grid.Row="0" Header="Connection Settings" Margin="0,0,0,10">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <Label Grid.Row="0" Grid.Column="0" Content="Site URL:" VerticalAlignment="Center"/>
                <TextBox x:Name="SiteUrlBox" Grid.Row="0" Grid.Column="1" Padding="5"/>
                
                <Label Grid.Row="1" Grid.Column="0" Content="Client ID:" VerticalAlignment="Center" Margin="0,5,0,0"/>
                <TextBox x:Name="ClientIdBox" Grid.Row="1" Grid.Column="1" Padding="5" Margin="0,5,0,0"/>
                
                <Label Grid.Row="2" Grid.Column="0" Content="Tenant ID:" VerticalAlignment="Center" Margin="0,5,0,0"/>
                <TextBox x:Name="TenantIdBox" Grid.Row="2" Grid.Column="1" Padding="5" Margin="0,5,0,0"/>
            </Grid>
        </GroupBox>
        
        <!-- Connection Button -->
        <Button x:Name="ConnectBtn" Grid.Row="1" Content="Connect to SharePoint" 
                HorizontalAlignment="Left" Padding="20,5" Margin="0,0,0,10" Click="ConnectBtn_Click"/>
        
        <!-- Library Selection -->
        <GroupBox Grid.Row="2" Header="Document Library" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal" Margin="10">
                <ComboBox x:Name="LibraryCombo" Width="400" VerticalAlignment="Center"/>
                <Button x:Name="RunBtn" Content="Run Version Trim" Margin="15,0,0,0" Padding="15,5" 
                        IsEnabled="False" Click="RunBtn_Click"/>
            </StackPanel>
        </GroupBox>
        
        <!-- Settings -->
        <GroupBox Grid.Row="3" Header="Trim Settings" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal" Margin="10">
                <Label Content="Keep Versions:" VerticalAlignment="Center"/>
                <TextBox x:Name="KeepVersionsBox" Text="10" Width="50" VerticalAlignment="Center" Padding="5"/>
                
                <Label Content="Page Size:" VerticalAlignment="Center" Margin="20,0,0,0"/>
                <TextBox x:Name="PageSizeBox" Text="100" Width="50" VerticalAlignment="Center" Padding="5"/>
                
                <CheckBox x:Name="WhatIfCheck" Content="What If Mode (Simulation Only)" 
                          IsChecked="True" VerticalAlignment="Center" Margin="20,0,0,0"/>
            </StackPanel>
        </GroupBox>
        
        <!-- Report Path -->
        <Grid Grid.Row="4" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" Content="Report Path:" VerticalAlignment="Center"/>
            <TextBox x:Name="ReportPathBox" Grid.Column="1" VerticalAlignment="Center" Padding="5" Margin="5,0"/>
            <Button x:Name="BrowseBtn" Grid.Column="2" Content="Browse..." Padding="10,5" Click="BrowseBtn_Click"/>
        </Grid>
        
        <!-- Log Output -->
        <GroupBox Grid.Row="5" Header="Log" Grid.RowSpan="2" Margin="0,0,0,10">
            <TextBox x:Name="LogBox" IsReadOnly="True" VerticalScrollBarVisibility="Auto" 
                     FontFamily="Consolas" FontSize="12" TextWrapping="Wrap"/>
        </GroupBox>
        
        <!-- Status Bar -->
        <StatusBar Grid.Row="6" VerticalAlignment="Bottom">
            <StatusBarItem>
                <TextBlock x:Name="StatusText" Text="Ready"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <ProgressBar x:Name="BusyBar" Width="150" Height="16" IsIndeterminate="False"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>